# Unreal Engine
Binaries/
Build/
DerivedDataCache/
Intermediate/
Saved/
.vs/
*.VC.db
*.opensdf
*.sdf
*.sln
*.suo
*.xcodeproj
*.xcworkspace

# Packaged builds
Builds/
Packaged/

# Source control friendly Unreal content (uncomment if using Perforce-style workflow instead)
# *.uasset filter=lfs diff=lfs merge=lfs -text
# *.umap filter=lfs diff=lfs merge=lfs -text

# Rider / VS Code
.idea/
.vscode/

# OS
Thumbs.db
.DS_Store

# Claude Code local settings
.claude/settings.local.json

# Stale duplicate project folder, superseded by "LazyRiver 5.8"
/LazyRiver/

# Oversized backup/export zip - exceeds GitHub's 100MB file limit
LazyRiver.zip
